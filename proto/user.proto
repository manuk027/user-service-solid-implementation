syntax = "proto3";

package user;

service UserService {
    rpc GetMe(GetMeRequest) returns (UserResponse);
}

message GetMeRequest {
    string userId = 1;
}

message UserResponse {
    string userId = 1;
    string name = 2;
    string email = 3;
    string role = 4;
}